# Option 1 (recommended for hosted DBs like Render/Neon/Supabase):
# DATABASE_URL=postgres://user:password@host:5432/database?sslmode=require

# Option 2 (local Postgres):
PORT=3001
PGHOST=localhost
PGPORT=5432
PGDATABASE=kims_dashboard
PGUSER=postgres
PGPASSWORD=postgres
# PGSSLMODE=require   # enable TLS if the server requires it

JWT_SECRET=kims-dashboard-jwt-secret-change-in-production
